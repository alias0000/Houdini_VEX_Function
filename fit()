//demonstration
Takes the value in one range and shifts it to the corresponding value in a new range.
받는 값은 old min ~ old max내에서의 value가 새로운 범위 new min ~ new max내에서 가지는 값이다.

//form
float fit(float value, float omin, float omax, float nmin, float nmax)
<vector> fit(vector value, vector omin, vector omax, vector nmin, vector nmax)

//example
fit(.3, 0, 1, 10, 20) == 13
0 ~ 1내에서 0.3의 값이 새로운 범위 10 ~ 20에서는 13이라는 값을 가지기 때문에 13을 반환한다.
